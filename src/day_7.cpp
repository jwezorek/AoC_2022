#include "util.h"
#include "days.h"
#include <range/v3/all.hpp>
#include <vector>
#include <string>
#include <iostream>

namespace r = ranges;
namespace rv = ranges::views;

/*------------------------------------------------------------------------------------------------*/

namespace {
}

void aoc::day_7(int day, const std::string& title) {
    //auto input = file_to_string(input_path(7, 1));
    std::cout << header(day, title);
    std::cout << "   part 1: " << 0 << "\n";
    std::cout << "   part 2: " << 0 << "\n";
}